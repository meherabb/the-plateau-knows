# Exact package versions recorded from the environment that produced every
# result in this repository (results/environment/environment.json).
# torch's +cu128 build tag is CUDA-version-specific; on a different CUDA
# setup, install a matching torch build from https://pytorch.org/get-started/locally/
# and the rest of this file will still apply.

torch==2.10.0+cu128
numpy==2.0.2
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.6.1
matplotlib==3.10.0
tqdm==4.67.3
ipython==7.34.0
notebook==6.5.7
nbformat==5.10.4
nbclient==0.5.13
